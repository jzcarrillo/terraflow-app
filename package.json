{
  "name": "terraflow-app",
  "version": "1.0.0",
  "description": "Terraflow Land Registry System - Test Suite",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:landregistry": "cd backend-landregistry && npm test",
    "test:users": "cd backend-users && npm test",
    "test:documents": "cd backend-documents && npm test",
    "test:blockchain": "cd backend-blockchain && npm test"
  },
  "private": true,
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
